<UserControl
    x:Class="TechSupport.UI.Views.EditableViews.EditDepartmentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:Background="White"
    mc:Ignorable="d">

    <Grid
        Width="300"
        Height="250"
        Margin="10">
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="3,10,3,2" />
            </Style>
        </Grid.Resources>
        <StackPanel>

            <Label Content="Название" FontFamily="Segoe Script"/>
            <TextBox Text="{Binding Department.Title}" />

            <Label Content="Категория" FontFamily="Segoe Script"/>
            <TextBox Text="{Binding Department.Place}" />

            <Label Content="Кол-во детей" FontFamily="Segoe Script"/>
            <TextBox Text="{Binding Department.Room}" />

        </StackPanel>
    </Grid>
</UserControl>
